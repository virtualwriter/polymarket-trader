# Archive

This directory holds legacy or dormant code that is not part of the USA production trader path.

- `addendum/` is a pre-auction simulation / market-model experiment from a separate project context.

Moving code here is a quarantine step only. Do not wire archived code into production wrappers or systemd units without an explicit operator request.
