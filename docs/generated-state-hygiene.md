# Generated State Hygiene

This document records the cleanup classification for tracked generated/state files. It is an audit only: do not untrack, delete, or move these files without a separate operator-approved state migration.

## Production Rule

The USA VPS wrapper, `scripts/run-polymarket-trader.sh`, is the current source of truth for state files that may be force-added and committed by production. Cleanup work must not fight that wrapper. If a file is in `DATA_FILES`, treat hourly churn as expected unless a separate migration changes the wrapper and is verified on the VPS.

## Keep Tracked For Recovery

These files are small enough to keep in Git and are used for recovery, learning, or auditability:

- `data/portfolio.json`
- `data/trades-detailed.csv`
- `data/trades-detailed-archive.csv`
- `data/position-snapshots.csv`
- `data/daily-macro.csv`
- `data/daily-valuations.csv`
- `data/signal-weights.json`
- `data/hypotheses.json`
- `data/learning-params.json`
- `data/blocked-signals.json`
- `data/processed-closed-trades.json`
- `data/learning-journal.md`
- `data/llm-close-rejections.json`
- `data/operationally-tainted-trades.json`

## Keep Tracked As Runtime Introspection

These are generated by the lean engine path and may churn during dry runs or hourly runs. They are useful for debugging and should be restored by `cleanup:harness` during cleanup tests:

- `data/engine-state.json`
- `data/llm-truth-state.json`
- `data/candidate-actions.json`
- `data/llm-advice.json`
- `data/execution-plan.json`

## Keep Tracked For Publication

These feed the static relative-value/heatmap output and are expected to change when the hourly report refreshes:

- `relative-value/index.html`
- `relative-value/cross_venue_relative_value.csv`
- `relative-value/latest.json`

## Generated Or Research Artifacts

These should not be included in USA cleanup commits unless the operator explicitly asks for a data-output commit:

- `relative-value/calibration/no_bias_candidates.jsonl`
- `relative-value/backtest_results.csv`
- `relative-value/backtest_summary.md`
- `relative-value/iv_variant_backtest.csv`
- `relative-value/iv_variant_backtest_4d.csv`
- `relative-value/iv_variant_backtest_7d.csv`
- `exports/`

New `relative-value/calibration/*.jsonl` files and `exports/` files are ignored going forward. Existing tracked generated files can still appear dirty because Git tracks them already.

## Cleanup Commit Rule

Before any cleanup commit:

1. Run `npm run prod:verify`.
2. Run `npm run cleanup:harness -- --out .runtime/cleanup-harness/<baseline>.json`.
3. Run `npm run cleanup:harness -- --compare .runtime/cleanup-harness/<baseline>.json`.
4. Stage source/docs only. Do not stage generated state unless the commit is explicitly a state-output commit.
5. Confirm `npm run prod:verify` reports no unresolved conflicts and no staged generated/Japan paths.

